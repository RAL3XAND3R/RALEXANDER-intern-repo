name: CI Checks

on:
  pull_request:

jobs:
  markdown-and-spell-check:
    runs-on: ubuntu-latest

    defaults:
      run:
        working-directory: duplicate-repo

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: duplicate-repo/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run Markdown lint
        run: npm run lint:md

      - name: Run spell check
        run: npm run spellcheck